/** @type {import('next').NextConfig} */
const nextConfig = {
 
  images: { unoptimized: true },
};

export default nextConfig; // if your file uses module.exports
// OR: export default nextConfig;  (if your file uses export default)
